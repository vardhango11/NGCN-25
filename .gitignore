 # =========================================================================
# A standard .gitignore file for Node.js & JavaScript projects.
# =========================================================================

# Dependencies
# ---------------------------------
# The node_modules directory contains all installed npm packages.
# It should always be ignored as it can be regenerated by running `npm install`.
node_modules/

# Logs
# ---------------------------------
# Log files are generated during runtime and are machine-specific.
logs
*.log
npm-debug.log*
yarn-error.log
pnpm-debug.log*

# Environment Variables
# ---------------------------------
# Ignore .env files containing sensitive credentials and API keys.
# Always commit a template file like .env.example instead.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Build Output
# ---------------------------------
# Compiled/transpiled code and build artifacts.
# Common for React (build), Vite (dist), or TypeScript (dist).
/build
/dist
.out/
.next/

# IDE & Editor Directories
# ---------------------------------
# Ignore metadata files generated by popular code editors.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.sublime-project
*.sublime-workspace

# Operating System Files
# ---------------------------------
# Ignore files generated by macOS, Windows, and Linux systems.
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# Other
# ---------------------------------
# Temporary files, test reports, and local caches.
.coverage
coverage/
.nyc_output/
.tmp